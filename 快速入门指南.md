# openGauss 快速入门和开发指南

## 1. 环境准备

### 1.1 系统要求
```bash
# 操作系统支持
- CentOS 7.6 (x86_64)
- openEuler 20.03/22.03/24.03 (aarch64/x86_64)

# 必需软件包
sudo yum install -y libaio-devel flex bison ncurses-devel glibc-devel \
    patch readline-devel libedit-devel libxml2-devel lz4-devel \
    numactl-devel unixODBC-devel java-1.8.0-openjdk-devel redhat-lsb-core
```

### 1.2 获取源代码
```bash
# 克隆主代码库
git clone https://gitee.com/opengauss/openGauss-server.git

# 获取第三方依赖库
wget https://opengauss.obs.cn-south-1.myhuaweicloud.com/latest/binarylibs/gcc10.3/openGauss-third_party_binarylibs_openEuler_x86_64.tar.gz
tar -zxvf openGauss-third_party_binarylibs_openEuler_x86_64.tar.gz
mv binarylibs ../
```

## 2. 编译构建

### 2.1 一键编译方式
```bash
cd openGauss-server

# Release版本编译
sh build.sh -m release -3rd ../binarylibs

# Debug版本编译
sh build.sh -m debug -3rd ../binarylibs

# 使用CMake方式编译
sh build.sh -m release -3rd ../binarylibs --cmake
```

### 2.2 手动编译方式
```bash
# 环境变量设置
export CODE_BASE=$(pwd)
export BINARYLIBS=../binarylibs
export GAUSSHOME=$CODE_BASE/dest/
export GCC_PATH=$BINARYLIBS/buildtools/gcc10.3
export CC=$GCC_PATH/gcc/bin/gcc
export CXX=$GCC_PATH/gcc/bin/g++
export LD_LIBRARY_PATH=$GAUSSHOME/lib:$GCC_PATH/gcc/lib64:$LD_LIBRARY_PATH
export PATH=$GAUSSHOME/bin:$GCC_PATH/gcc/bin:$PATH

# 配置和编译
./configure --gcc-version=10.3.1 CC=g++ CFLAGS="-O2 -g3" \
  --prefix=$GAUSSHOME --3rd=$BINARYLIBS \
  --enable-thread-safety --with-readline --without-zlib

make -sj && make install -sj
```

## 3. 部署和初始化

### 3.1 配置文件准备
```xml
<!-- clusterconfig.xml 示例 -->
<?xml version="1.0" encoding="UTF-8"?>
<ROOT>
    <CLUSTER>
        <PARAM name="clusterName" value="dbCluster" />
        <PARAM name="nodeNames" value="node1" />
        <PARAM name="backIp1s" value="127.0.0.1"/>
        <PARAM name="gaussdbAppPath" value="/opt/opengauss/app" />
        <PARAM name="gaussdbLogPath" value="/var/log/opengauss" />
        <PARAM name="tmpMppdbPath" value="/tmp/opengauss"/>
        <PARAM name="gaussdbToolPath" value="/opt/opengauss/om" />
        <PARAM name="corePath" value="/opt/opengauss/corefile"/>
        <PARAM name="clusterType" value="single-inst"/>
    </CLUSTER>
    <DEVICELIST>
        <DEVICE sn="1000001">
            <PARAM name="name" value="node1"/>
            <PARAM name="backIp1" value="127.0.0.1"/>
            <PARAM name="sshIp1" value="127.0.0.1"/>
            <PARAM name="dataNum" value="1"/>
            <PARAM name="dataPortBase" value="5432"/>
            <PARAM name="dataNode1" value="/opt/opengauss/data/node1"/>
        </DEVICE>
    </DEVICELIST>
</ROOT>
```

### 3.2 环境初始化
```bash
# 创建安装目录
sudo mkdir -p /opt/opengauss
sudo chown $USER:$USER /opt/opengauss

# 环境预配置
cd script
./gs_preinstall -U omm -G dbgrp -X clusterconfig.xml

# 切换到数据库用户
su - omm
```

### 3.3 数据库安装
```bash
# 执行安装
gs_install -X /path/to/clusterconfig.xml

# 验证安装
gsql -d postgres -p 5432 -c "SELECT version();"
```

## 4. 核心代码学习路径

### 4.1 系统启动流程
```
main() → PostmasterMain() → BackendStartup() → PostgresMain()
   ↓           ↓                ↓                ↓
入口函数    主进程管理    后端进程创建      查询处理循环
```

**关键文件追踪:**
1. `src/gausskernel/process/main/main.cpp` - 程序入口
2. `src/gausskernel/process/postmaster/postmaster.cpp` - 主进程
3. `src/gausskernel/tcop/postgres.cpp` - 协议处理
4. `src/gausskernel/executor/execMain.cpp` - 执行器

### 4.2 存储引擎学习
```
Buffer Manager → Access Method → Storage Manager
     ↓               ↓               ↓
缓冲池管理      索引/表访问      物理存储管理
```

**核心文件:**
- `src/gausskernel/storage/buffer/bufmgr.cpp` - 缓冲管理
- `src/gausskernel/storage/access/heap/heapam.cpp` - 堆表访问
- `src/gausskernel/storage/smgr/smgr.cpp` - 存储管理

### 4.3 事务处理学习
```
Transaction Start → MVCC Control → WAL Logging → Commit/Rollback
      ↓                ↓             ↓              ↓
事务开始        多版本控制      日志记录        事务结束
```

**关键文件:**
- `src/gausskernel/storage/access/transam/xact.cpp` - 事务管理
- `src/gausskernel/storage/access/transam/clog.cpp` - 提交日志
- `src/gausskernel/storage/access/transam/xlog.cpp` - WAL日志

## 5. 开发实践示例

### 5.1 自定义函数开发
```cpp
// 创建扩展目录
mkdir contrib/my_extension
cd contrib/my_extension

// my_extension.c
#include "postgres.h"
#include "fmgr.h"

PG_MODULE_MAGIC;

PG_FUNCTION_INFO_V1(my_custom_function);

Datum
my_custom_function(PG_FUNCTION_ARGS)
{
    int32 arg = PG_GETARG_INT32(0);
    PG_RETURN_INT32(arg * 2);
}

// Makefile
MODULES = my_extension
EXTENSION = my_extension
DATA = my_extension--1.0.sql

PG_CONFIG = pg_config
PGXS := $(shell $(PG_CONFIG) --pgxs)
include $(PGXS)
```

### 5.2 索引访问方法开发
```cpp
// 自定义索引访问方法
typedef struct {
    IndexAmRoutine amroutine;
    // 自定义字段
} MyIndexAmRoutine;

// 实现核心回调函数
static IndexBuildResult *
myambuild(Relation heap, Relation index, IndexInfo *indexInfo)
{
    // 构建索引的实现
    return NULL;
}

static bool
myaminsert(Relation index, Datum *values, bool *isnull,
           ItemPointer ht_ctid, Relation heapRel,
           IndexUniqueCheck checkUnique)
{
    // 插入索引项的实现
    return true;
}
```

### 5.3 执行器节点扩展
```cpp
// 自定义执行节点
typedef struct MyCustomScanState {
    ScanState ss;
    // 自定义状态信息
    void *custom_data;
} MyCustomScanState;

static TupleTableSlot *
ExecMyCustomScan(PlanState *pstate)
{
    MyCustomScanState *node = castNode(MyCustomScanState, pstate);
    TupleTableSlot *slot = node->ss.ss_ScanTupleSlot;
    
    // 自定义扫描逻辑
    if (has_more_data()) {
        // 填充slot
        ExecStoreTuple(tuple, slot, InvalidBuffer, false);
        return slot;
    }
    
    return NULL;
}
```

## 6. 调试和性能分析

### 6.1 调试技巧
```bash
# 启用调试构建
sh build.sh -m debug -3rd ../binarylibs

# 使用GDB调试
gdb --args gaussdb -D /path/to/data/directory

# 内存检查模式
sh build.sh -m memcheck -3rd ../binarylibs
```

### 6.2 性能监控
```sql
-- 查看活动会话
SELECT * FROM pg_stat_activity;

-- 查看等待事件
SELECT * FROM pg_stat_activity WHERE wait_event IS NOT NULL;

-- 查看缓冲池统计
SELECT * FROM pg_stat_bgwriter;

-- 查看表和索引统计
SELECT * FROM pg_stat_user_tables;
```

### 6.3 日志分析
```bash
# 启用详细日志
echo "log_statement = 'all'" >> postgresql.conf
echo "log_min_duration_statement = 0" >> postgresql.conf

# 查看执行计划
EXPLAIN (ANALYZE, BUFFERS) SELECT * FROM my_table WHERE id = 1;
```

## 7. 常见问题解决

### 7.1 编译问题
```bash
# 检查依赖
ldd dest/bin/gaussdb

# 清理重新编译
make clean && make -sj

# 检查版本兼容性
./src/get_PlatForm_str.sh
```

### 7.2 运行时问题
```bash
# 检查共享内存
ipcs -m

# 检查信号量
ipcs -s

# 增加系统限制
echo "kernel.shmmax = 2147483648" >> /etc/sysctl.conf
sysctl -p
```

### 7.3 性能调优
```sql
-- 调整内存参数
ALTER SYSTEM SET shared_buffers = '2GB';
ALTER SYSTEM SET work_mem = '64MB';
ALTER SYSTEM SET maintenance_work_mem = '512MB';

-- 重启使配置生效
SELECT pg_reload_conf();
```

## 8. 学习资源推荐

### 8.1 官方文档
- [openGauss官方文档](https://opengauss.org/zh/docs/)
- [开发者指南](https://opengauss.org/zh/docs/developer-guide/)
- [源码解析系列博客](https://opengauss.org/zh/blogs/)

### 8.2 社区资源
- GitHub/Gitee源码仓库
- 开发者邮件列表
- 在线技术论坛

### 8.3 实践建议
1. **循序渐进**: 从简单的功能模块开始学习
2. **动手实践**: 修改现有代码验证理解
3. **阅读文档**: 结合官方文档深入理解
4. **参与社区**: 积极参与讨论和贡献

通过这个指南，您可以快速掌握openGauss的核心概念和开发技能，为进一步深入研究打下坚实基础。